# hameed-discovery-service
Eureka service registry — every other Hameed restaurant microservice registers here so they can find each other by name instead of hardcoded host:port. NOT included in the original upload; added so the full stack is actually runnable end-to-end. 
